# Machine A field receipt — 2026-08-03

Status: **partial**. This run establishes the first-machine baseline. It does not satisfy the multi-machine release gate.

Evidence labels in this receipt are literal: a local test run is called a local test run; a client configuration listing is not called a connection; a GitHub secret scan is not called test verification.

Observed at `2026-08-03T12:57:09Z` on `machine-a`:

- macOS `26.5.2`, Apple Silicon
- Python `3.14.6`
- Node.js `22.21.1`
- Codex CLI `0.145.0`
- Claude Code `2.1.220`
- Gemini CLI `0.38.2`
- Worker version `edbd8c95-a671-44bf-83a8-ad8d688f6777`

## Local evidence

Allocated size from `du -sk`:

| Layer | KiB |
|---|---:|
| Claude transcripts | 3,898,240 |
| Active Codex transcripts | 1,118,228 |
| Archived Codex transcripts | 5,293,988 |
| Gemini retained sessions | 27,948 |
| `recall.db` | 35,928 |

The transcript corpus occupies about 9.9 GiB. The distilled database occupies about 35 MiB. These are different retention domains and are not reported as one workspace total.

The ingestion watermark contains:

| Client | Files covered |
|---|---:|
| Claude | 1,536 |
| Codex | 682 |
| Gemini | 80 |

The durable cloud outbox contains 16 acknowledged events, zero pending retries, and 20,474 bytes of staged JSON payloads. Cursor values were `voice_signals=16400` and `recipes=14`.

## Client connectivity

- Claude Code reported `recall-context` connected to the deployed MCP endpoint.
- Gemini CLI reported `recall-context` connected through the authenticated bridge.
- Codex listed `recall_context` enabled with secret values masked. This is configuration evidence only; the same-memory retrieval check remains open.

No client has yet produced the five processed real-session candidates required by the release gate.

## Remote state

A read-only D1 query returned:

| Record | Count |
|---|---:|
| Stale memories | 17 |
| Processed ingest receipts | 17 |
| `candidate_ingested` events | 17 |
| `status_changed` events | 17 |

The source labels are 16 Gemini catch-up candidates and one Codex synthetic smoke record. All remain Stale. There are zero Candidate and zero Approved records.

D1 reported 143,360 bytes after the query. The approved-memory R2 snapshot was 76 bytes and contained zero memories, matching D1.

## Retention dry run

`python3 poe-extract.py retention-report --grace-days 7` inspected archived Codex transcripts only:

| Classification | Files | Bytes |
|---|---:|---:|
| Watermark covered and older than seven days | 68 | 2,039,233,157 |
| Covered but still inside the grace window | 179 | 3,381,229,542 |
| Uncovered | 0 | 0 |
| Changed after ingest | 0 | 0 |
| Read errors | 0 | 0 |

The command returned `deletion_authorized: false`. No transcript was deleted. Watermark coverage proves ingestion only; project-truth promotion and archive safety still require human closeout.

## Worktree output

The recall repository has no linked worktrees and no reclaimable build directories. The previously measured local-meeting-notes hotspot currently has 40 linked worktrees and zero build directories eligible for reaping. This is a separate cleanup system; worktree output is not counted as memory growth.

## Open gates

- run the same receipt on a second physical machine with its own outbox
- process at least five real candidates from each of Claude, Codex, and Gemini
- prove offline retry and later acknowledgment without duplication
- review at least ten candidates across Approved, Contradicted, Superseded, and Stale
- retrieve the same Approved record and omit the same non-Approved record in all three harnesses
- prove a supersession chain, D1 recovery, and regenerated R2 snapshot
- compare Machine A and Machine B storage after the field interval

The repository rename, merge, release tag, and reflective blog remain blocked on those receipts.

## Privacy audit correction

A later read-only audit, prompted by Machine B's stop-before-push finding, showed
that the prototype had persisted `provenance.evidence_excerpt` in D1:

| D1 records | Status | Source | Excerpts present | Character range |
|---:|---|---|---:|---:|
| 1 | Candidate | Claude `voice_signals` | 1 | 245 |
| 16 | Stale | Gemini `voice_signals` | 16 | 27–1,200 |
| 1 | Stale | Codex `recipes` | 0 | n/a |

The aggregate query returned no excerpt text and reported zero rows written.
The live R2 snapshot was downloaded to an isolated temporary directory and
contained zero Approved memories, zero excerpts, and 76 bytes. The temporary
copy was removed after inspection.

This invalidates the earlier broad statement that raw transcript fields stayed
local. The candidate body was within the intended cloud boundary; the whole
message copied into `evidence_excerpt` was not. Forward ingestion remains
stopped until the Worker rejects that field and existing outboxes are sanitized.
Removing the 17 existing D1 excerpts is a separate destructive remediation and
requires an approved, audited migration.

The forward-only Worker guard was subsequently deployed as version
`92707d40-85e6-41d6-ad2d-bdd2ef804ae0`. Its public health response returned
that exact ID, and an authenticated synthetic event containing
`provenance.evidence_excerpt` returned HTTP 400 before enqueue. This prevents
new exposure but does not remove the 17 existing D1 excerpts.
